{
  "name": "client-app",
  "version": "1.0.0",
  "description": "A client-side web application.",
  "scripts": {
    "build": "mkdir -p dist && postcss src/input.css -o dist/style.css"
  },
  "devDependencies": {
    "tailwindcss": "3.3.3",
    "postcss": "8.4.31",
    "autoprefixer": "10.4.16",
    "postcss-cli": "10.1.0"
  }
}
